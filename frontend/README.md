# Docker Copilot Angular

Angular management console for Docker Copilot, based on the TailAdmin Angular + Tailwind dashboard layout direction.

```bash
npm install
npm start
```

The production build writes browser assets to `dist/browser`. The backend CI copies that directory to `front/` for Go embedding.
